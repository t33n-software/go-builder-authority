# Builder Policy Boundary

This directory will contain versioned policy references for approved Go
builder inputs, evidence, promotion, consumer admission, and revocation.

It must never contain credentials, private keys, tokens, tenant runtime
configuration, or mutable builder references.
